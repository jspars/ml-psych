# Netlify build configuration for lawsonpsych.com
# Node 22 (LTS) required: Vite 8 needs ^20.19 || >=22.12
[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "22"
